"""SQLite-backed demo uses dictionaries at the API boundary.

This file is intentionally small so the app remains swappable to SQLAlchemy/Postgres later without leaking ORM objects into answer logic.
"""
